/**
 * Basic configuration for servers. For now we have
 * only the refresh rate for the ServerMonitor.
 */

const config = {
    monitor: {
        refresh: 1 * 1000
    }
}

module.exports = config